import React from "react";

interface ClickButton {
    name: string;
    color: string;
    tags?: string;
}

export default function Button() {
  return (
    <div>
      <button></button>
    </div>
  );
}
